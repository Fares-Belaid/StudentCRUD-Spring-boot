spring:
  datasource:
    url: jdbc:mysql://localhost:3308/Student?createDatabaseIfNotExist=true&userSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDateTimeCode=false&serverTimezone=UTC
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
    
 ## Specify DBMS:
  jpa:
    database: MYSQL
    show-sql: false
    
# Hibernate DDL Auto (create, create-drop,update)
    hibernate:
      ddl-auto: update
    properties:
      dialect : org.hibernate.dialect.MySQLDiaclect
server:
  port: 8081
